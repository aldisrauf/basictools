% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_gt_like_excel.R
\name{export_gt_like_excel}
\alias{export_gt_like_excel}
\title{Export Data Frame to Excel with gt-like Grouped Headers}
\usage{
export_gt_like_excel(
  df,
  delim = "_",
  sheet = "Sheet1",
  start_row = 3,
  table_caption = "",
  wb
)
}
\arguments{
\item{df}{Data frame. The data to be exported. Column names should use the `delim` to indicate levels.}

\item{delim}{Character. The separator used in column names to define header levels. Default is "_".}

\item{sheet}{Character. The name of the worksheet to create. Default is "Sheet1".}

\item{start_row}{Numeric. The row number where the table (including headers) should start. Default is 3.}

\item{table_caption}{Character. An optional caption to be placed at the top-left (Row 1). Default is "".}

\item{wb}{An openxlsx workbook object.}
}
\value{
The modified workbook object (`wb`).
}
\description{
This function exports a data frame to an Excel worksheet with support for multi-level
column headers (mimicking the style of the 'gt' package). It automatically merges
cells horizontally and vertically based on a delimiter in the column names.
}
\examples{
\dontrun{
library(openxlsx)
wb <- createWorkbook()
df <- data.frame("Group A_Sub 1" = 1:5, "Group A_Sub 2" = 6:10, "Group B_Sub 1" = 11:15)
wb <- export_gt_like_excel(df, wb = wb, table_caption = "Sample Table")
saveWorkbook(wb, "output.xlsx", overwrite = TRUE)
}
}
